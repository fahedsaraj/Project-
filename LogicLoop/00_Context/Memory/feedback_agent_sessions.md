type: feedback
Each agent works and is trained in its own chat session. Agents edit only their own role file, log, and memory files (prefix memory files with the agent name); pull before pushing; shared files (Approvals_Log.md) get small, immediate commits. Automation connecting the agents is built after training.
